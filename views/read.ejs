<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
            <title>게시글</title>
    </head>
    <body>
        <h1 align = "center">게시글</h1>
        <h3 align = "center"><%= rows[0].title %></h3>
        <hr>
        <hr>
        <table width="100%" padding="4px" cellspacing="15px">
            <tr>
                <td width="10%" height="50px">작성자</td>
                <td width="90%" height="50px"><%= rows[0].username %></td>
            </tr>
            <tr>
                <td width="10%" height="70px">제목</td>
                <td width="90%" height="70px"><%= rows[0].title %></td>
            </tr>
            <tr>
                <td width="10%" height="70px">내용</td>
                <td width="90%" height="70px"><%= rows[0].detail %></td>
            </tr>
            <tr>
                <td width="10%" height="70px">등록일</td>
                <td width="90%" height="50px"><%= rows[0].todate %></td>
            </tr>
        </table>
        <hr>

        <a href="/1">목록</a>
        <a href="/read/delete/<%= rows[0].userID %>">삭제</a>

        <form action="/read/<%= rows[0].userID %>" method='post'>
            <input type="hidden" name="userID" value="<%= rows[0].userID %>">
            <p>
                <td>이름</td>
                <input type="text" name="commname" placeholder="이름을 입력해주세요.">
            </p>
            </p>
                <td>댓글</td>
                <input type="text" name="comm" placeholder="댓글을 작성해주세요.">
            </p>
            <p>
                <input type="submit" value="댓글쓰기" location.href="/read/<%= rows[0].userID %>">
            </p>
        </form>

        <% for (var i=0;i<length;i++){ %>
            <table>
                <tr>
                    <td>작성자</td>
                    <td><%= arr[i].commname %></td>
                </tr>
                <tr>
                    <td>내용</td>
                    <td><%= arr[i].comm %></td>
                </tr>
                <tr>
                    <td>등록일</td>
                    <td><%= arr[i].todate %></td>
                </tr>
                <a href="/read/delete2/<%= arr[i].commID %>">삭제</a>
            </table>
            <hr>
        <% } %>
    </body>

</html>
